{
  "name": "avsm-prints",
  "version": "2.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "mkdir -p src public && cp App.jsx src/ && cp index.js src/ && cp index.html public/ && cp *.jpg public/ 2>/dev/null; cp *.png public/ 2>/dev/null; cp *.mp4 public/ 2>/dev/null; cp manifest.json public/ 2>/dev/null; react-scripts build"
  },
  "browserslist": {
    "production": [">0.2%","not dead"],
    "development": ["last 1 chrome version"]
  }
}
